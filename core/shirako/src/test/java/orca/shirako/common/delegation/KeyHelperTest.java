package orca.shirako.common.delegation;

import orca.shirako.container.OrcaTestCase;

public class KeyHelperTest extends OrcaTestCase
{
    // FIXME: fails on the build server
    public void test() throws Exception
    {
/*        KeyHelper helper = new KeyHelper();
        helper.initialize();
        
        System.out.println("The actor certificate is: " + helper.getKeystore().getActorCertificate());
    */
    }
}