This directory contains requests that exercise various features in intra-site embedding. 

NOTE: Before submitting these requests, please change the domains of node, nodegroups, storage etc. to the domain of the rack you are testing.

complex-vm-topology.rdf : Tests complex topology with vms and links. This is TS2.2.

node-nodegroup.rdf : Tests vm connected to a nodegroup, with postboot scripts. Test modify and extendwith it.

node-nodegroup-storage.rdf : Tests above with storage.
 
vm-baremetal-storage.rdf : Tests vm with storage, baremetal with storage and a vm topology with nodegroup.
